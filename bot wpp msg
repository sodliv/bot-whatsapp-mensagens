import pandas as pd
import time
import webbrowser
import pyautogui
import keyboard
import urllib.parse

# Carrega os dados do Excel
df = pd.read_excel(r'C:\\Users\\smlima\\Desktop\\Contatos_dependentes - Copia.xlsx')

print("Iniciando envio de mensagens...")

# Mensagem base com personalização
mensagem_base = """Olá, {nome}. O prazo para envio da foto está encerrando!

Para isso, pedimos, por gentileza, que você:
* Envie uma foto sua com seu(s) filho(s);
* Complete a frase "Ser pai é...";
* Informe seu nome, o nome do(s) seu(s) filho(s) e a unidade Bundy.

📅 Nos envie as informações até 31/07, pelo Google Forms.

É só clicar no link abaixo:
https://forms.gle/1y4Xqkm9RvvQEPnC9

Aguardamos sua resposta para tornar seu dia dos pais ainda mais especial! 🥰"""

# Loop por cada contato
for index, row in df.iterrows():
    nome = row['Nome']
    telefone = row['Telefone']

    # Personaliza a mensagem
    mensagem = mensagem_base.format(nome=nome)

    # Codifica a mensagem corretamente com todos os caracteres
    mensagem_codificada = urllib.parse.quote(mensagem)

    # Gera o link do WhatsApp com a mensagem codificada
    link = f"https://web.whatsapp.com/send?phone={telefone}&text={mensagem_codificada}"

    # Abre o navegador com o link
    webbrowser.open(link)

    print(f"Aguardando carregamento do WhatsApp para {nome}...")
    time.sleep(10)

    # Pressiona Enter para enviar a mensagem
    pyautogui.press("enter")

    print(f"Mensagem enviada para {nome} ({telefone})")

    time.sleep(5)  # Espera um pouco antes de passar para o próximo
    keyboard.press_and_release('ctrl+w')  # Fecha a aba atual

print("✅ Todas as mensagens foram enviadas com sucesso!")
